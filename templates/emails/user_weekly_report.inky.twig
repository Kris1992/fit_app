{% extends 'emails/base_email.inky.twig' %}

{% block content %}
        <row>
            <columns>
                <spacer size="16"></spacer>
                <p>
                    <h4>Hello <strong>{{ email.toName }}</strong>!</h4>
                </p>
                <p>
                    Your weekly workout report is ready!! Check it out now!
                </p>
            </columns>
        </row>
        <row>
            <columns>
                {{ include('emails/workout_report_table.html.twig', { 'table' : 'report-table' }) }}
            </columns>
        </row>
        <row>
            <columns>
                <center>
                    <spacer size="20"></spacer>
                    <button class="button large rounded" href="{{ url('app_homepage') }}">More statistics on FitApp</button>
                    <spacer size="20"></spacer>
                </center>
            </columns>
        </row>
{% endblock %}
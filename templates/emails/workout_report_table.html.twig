<table class="{{ table }}">
    <thead>
        <tr>
            <th>Activity</th>
            <th>Duration</th>
            <th>Burnout</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        {% for workout in workouts %}
            <tr>
                <td>{{ workout.activity.name }}</td>
                <td>{{ workout.durationSecondsTotal | time }}</td>
                <td>{{ workout.burnoutEnergyTotal }}</td>
                <td>{{ workout.startAt | date('Y-m-d H:i') }}</td>
            </tr>
        {% endfor %}
    </tbody>
    <tfoot>
        <tr>
            <td>Total</td>
            <td></td>
            <td>{{ TotalEnergy }}</td>
            <td>{{ workouts|length }}</td>
        </tr>
    </tfoot>
</table>


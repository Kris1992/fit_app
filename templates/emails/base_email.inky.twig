{% apply inky_to_html|inline_css(source('@styles/foundation-emails.css'), source('@styles/_email.css')) %}

<container>
    <row class="collapse bg-primary">
        <columns>
            <a href="{{ url('app_homepage') }}">
                <img class="brand-icon" src="{{ absolute_url(asset('img/logo.png')) }}" width="30" height="30" alt="FitApp">
                <span class="text-white">FitApp</span>
            </a>
        </columns>
    </row>
</container>
<container>
    {% block content %}
    {% endblock %}
    <row>
        <columns>
                <p>See You during next workout,</p>
                <p>
                    <strong>
                        The FitApp Team
                    </strong>
                </p>
        </columns>
    </row>
    <hr/>
    <row class="footer">
        <columns>
            <p class="text-center">
                <small>Problems or questions??<a href="#">Contact us</a></small>
            </p>
        </columns>
    </row>
</container>
{% endapply %}
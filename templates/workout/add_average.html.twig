{% extends 'content_base.html.twig' %}

{% block title %}Add Workout{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-datetimepicker/2.5.20/jquery.datetimepicker.min.css">
    <link rel="stylesheet" href="{{ asset('css/_login.css') }}">
{% endblock %}
{% block section_class %}addWorkout{% endblock %} 
{% block content_class %}{% endblock %}
{% block content_body %}
	      	<div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
		        <div class="card card-signin my-5 shadow-box">
                    <ul class="nav nav-pills nav-fill">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('workout_add_average') }}">Average data</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('workout_add_specific') }}">Specific data</a>
                        </li>
                    </ul>
                    {{ include('forms/workout_average_data_form.html.twig', {
                        header_text: 'Add new Workout',
                        button_text: 'Add'
                    }) }}
		        </div>
	      	</div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-datetimepicker/2.5.20/jquery.datetimepicker.full.min.js"></script>
    <script type="module" src="{{ asset('js/_formHelper.js') }}"></script>
    <script type="module" src="{{ asset('js/_workoutSets.js') }}"></script>
    <script>
    $(document).ready(function() {
        
        $('.js-datepicker').datetimepicker({
            format:'Y-m-d H:i'
        });
    });
    </script>
{% endblock %}